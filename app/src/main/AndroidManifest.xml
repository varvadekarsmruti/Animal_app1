<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smrutimukundvarvadekar.animal_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">

        </activity>
        <activity android:name=".FirstDayAtFarmActivity" />
        <activity android:name=".FeedIntakeActivity" />
        <activity android:name=".YieldAtFarmActivity" />
        <activity android:name=".ClimateAtFarmActivity" />
        <activity android:name=".RestingActivity" />
        <activity android:name=".BuyAnimalActivity">
            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
    </application>

</manifest>